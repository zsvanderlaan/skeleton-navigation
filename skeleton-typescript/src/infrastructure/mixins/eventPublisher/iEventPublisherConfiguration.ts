import {IConfiguration} from "../configuration/iConfiguration";
import {IEventPublisher} from "./iEventPublisher";

export interface IEventPublisherConfiguration extends IConfiguration<IEventPublisher> {
  
}
